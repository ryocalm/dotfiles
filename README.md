## dotfiles

```sh
# shell utilities
$ brew install peco
$ brew install ghq

# GPG
$ brew install gnupg
$ brew install gpg-agent pinentry-mac

# programming language
$ brew install go

# editor
$ brew install emacs
$ brew install vim

# shell commands
$ brew install tree
$ brew install bat
```